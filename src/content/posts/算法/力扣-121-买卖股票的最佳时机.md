---
id: "力扣-121-买卖股票的最佳时机"
title: "动态规划"
excerpt: "一次遍历贪心：维护历史最低买入价，用当前价减去最低价更新最大利润。"
date: "2024-05-24"
readTime: "1 min"
tags: ["算法"]
emoji: "💻"
---
> 一次遍历贪心：维护历史最低买入价，每次用当前价格减去最低价更新最大利润。

## 动态规划

```c++
class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int minPrice = INT_MAX, maxProfit = 0;
        for (int price: prices) {
            maxProfit = max(maxProfit, price - minPrice);
            minPrice = min(price, minPrice);
        }
        return maxProfit;
    }
};
```
