---
id: "力扣-516-最长回文子序列"
title: "区间背包dp"
excerpt: "区间 DP：dp[i][j] 表示 s[i..j] 内的最长回文子序列长度。"
date: "2024-06-18"
readTime: "1 min"
tags: ["算法"]
emoji: "💻"
---
> 区间 DP：dp[i][j] 表示 s[i..j] 的最长回文子序列长度，两端字符相等则加 2，否则取去掉任一端后的较大值。

## 区间背包dp

```c++
int dp[1010][1010];
class Solution {
public:
    int longestPalindromeSubseq(string s) {
    for (int i = 0; i < s.length(); ++i) {
        dp[i][i] = 1;
    }
    for (int i = 1; i < s.length(); ++i) {
        int t = 0;
        for (int j = 0; j + i < s.length(); ++j) {
            if (s[j] == s[j + i]) {
                t = dp[j + 1][j + i - 1]  + 2;
            } else {
                t = max(dp[j  + 1][j + i], dp[j][j + i - 1]);
            }
            dp[j][j + i] = t;
        }
    }
    return dp[0][s.length() - 1];
    }
};
```
